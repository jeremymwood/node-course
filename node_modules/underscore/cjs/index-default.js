var index = require('underscore/cjs');
var mixin = require('underscore/cjs/mixin.js');

// Default Export

// Add all of the Underscore functions to the wrapper object.
var _ = mixin(index);
// Legacy Node.js API.
_._ = _;

module.exports = _;
