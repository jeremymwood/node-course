import tagTester from 'underscore/modules/_tagTester.js';
import isFunction from 'underscore/modules/isFunction.js';
import isArrayBuffer from 'underscore/modules/isArrayBuffer.js';
import { hasStringTagBug } from 'underscore/modules/_stringTagBug.js';

var isDataView = tagTester('DataView');

// In IE 10 - Edge 13, we need a different heuristic
// to determine whether an object is a `DataView`.
function ie10IsDataView(obj) {
  return obj != null && isFunction(obj.getInt8) && isArrayBuffer(obj.buffer);
}

export default (hasStringTagBug ? ie10IsDataView : isDataView);
