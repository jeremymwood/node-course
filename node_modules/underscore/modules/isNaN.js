import { _isNaN } from 'underscore/modules/_setup.js';
import isNumber from 'underscore/modules/isNumber.js';

// Is the given value `NaN`?
export default function isNaN(obj) {
  return isNumber(obj) && _isNaN(obj);
}
