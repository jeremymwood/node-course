import identity from 'underscore/modules/identity.js';
import isFunction from 'underscore/modules/isFunction.js';
import isObject from 'underscore/modules/isObject.js';
import isArray from 'underscore/modules/isArray.js';
import matcher from 'underscore/modules/matcher.js';
import property from 'underscore/modules/property.js';
import optimizeCb from 'underscore/modules/_optimizeCb.js';

// An internal function to generate callbacks that can be applied to each
// element in a collection, returning the desired result — either `_.identity`,
// an arbitrary callback, a property matcher, or a property accessor.
export default function baseIteratee(value, context, argCount) {
  if (value == null) return identity;
  if (isFunction(value)) return optimizeCb(value, context, argCount);
  if (isObject(value) && !isArray(value)) return matcher(value);
  return property(value);
}
